/// Min priority supported by library.
///
/// Currently, priorities are defined by a non-negative integer.
const int kMinPriority = 0;

/// Max priority supported by library
///
/// Currently, priorities are defined by a non-negative integer.
const int kMaxPriority = 100;

/// Default priority value
///
/// Currently, `kDefaultPriority = kMinPriority`, so default is always the
/// lowest.
const int kDefaultPriority = kMinPriority;
